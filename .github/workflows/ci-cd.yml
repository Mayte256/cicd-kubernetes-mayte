name: CI/CD Pipeline con Kubernetes

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build-test-deploy-kubernetes:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout del código
      uses: actions/checkout@v3
    
    - name: 📋 Verificar estructura de archivos
      run: |
        echo "=== Archivos en el repositorio ==="
        ls -la
        echo ""
        echo "=== Contenido de requirements.txt ==="
        cat requirements.txt
        echo ""
    
    - name: 🐍 Configurar Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: 📦 Instalar dependencias Python
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then
          pip install -r requirements.txt
          echo "✅ Dependencias instaladas correctamente"
        else
          echo "❌ ERROR: No se encuentra requirements.txt"
          exit 1
        fi
    
    - name: ✅ Validar código Python
      run: |
        python -m py_compile app.py
        echo "✅ Sintaxis de Python válida"
    
    - name: 🧪 Ejecutar tests unitarios
      run: |
        python -c "from app import app; print('✅ App importada correctamente')"
        python -c "from app import app; client = app.test_client(); response = client.get('/health'); assert response.status_code == 200; print('✅ Health check funciona')"
        echo "✅ Todos los tests pasaron"
    
    - name: 🐳 Construir imagen Docker
      run: |
        docker build -t cicd-kubernetes-mayte:latest .
        docker images | grep cicd-kubernetes-mayte
        echo "✅ Imagen Docker construida exitosamente"
    
    - name: 🧪 Test rápido del contenedor Docker
      run: |
        echo "Probando contenedor Docker..."
        docker run -d --name test-container -p 5000:5000 cicd-kubernetes-mayte:latest
        sleep 10
        curl -f http://localhost:5000/health || echo "Container no está listo todavía"
        docker logs test-container
        docker stop test-container
        docker rm test-container
        echo "✅ Test de contenedor completado"
    
    - name: ☸️ Instalar Minikube
      run: |
        curl -LO https://storage.googleapis.com/minikube/releases/latest/minikube-linux-amd64
        sudo install minikube-linux-amd64 /usr/local/bin/minikube
        minikube version
        echo "✅ Minikube instalado"
    
    - name: ☸️ Iniciar cluster de Kubernetes
      run: |
        minikube start --driver=docker --cpus=2 --memory=2048
        minikube status
        echo "✅ Cluster de Kubernetes iniciado"
    
    - name: ☸️ Configurar kubectl
      uses: azure/setup-kubectl@v3
      with:
        version: 'latest'
    
    - name: ☸️ Verificar cluster
      run: |
        kubectl cluster-info
        kubectl get nodes
        echo "✅ Cluster funcionando"
    
    - name: ☸️ Cargar imagen en Minikube
      run: |
        minikube image load cicd-kubernetes-mayte:latest
        echo "✅ Imagen cargada en Minikube"
    
    - name: ☸️ Desplegar en Kubernetes
      run: |
        kubectl apply -f kubernetes/deployment.yaml
        kubectl apply -f kubernetes/service.yaml
        echo "✅ Manifiestos aplicados"
    
    - name: ⏳ Esperar pods listos
      run: |
        echo "Esperando que los pods estén listos..."
        kubectl wait --for=condition=ready pod -l app=cicd-kubernetes-mayte --timeout=120s || true
        kubectl get pods -l app=cicd-kubernetes-mayte
    
    - name: 📊 Estado del despliegue
      run: |
        echo "=== PODS ==="
        kubectl get pods -l app=cicd-kubernetes-mayte -o wide
        echo ""
        echo "=== DEPLOYMENT ==="
        kubectl get deployment cicd-kubernetes-mayte
        echo ""
        echo "=== SERVICE ==="
        kubectl get service cicd-kubernetes-mayte-service
        echo ""
        echo "=== DESCRIBE PODS ==="
        kubectl describe pods -l app=cicd-kubernetes-mayte
    
    - name: 🧪 Probar aplicación en Kubernetes
      run: |
        echo "Obteniendo nombre del pod..."
        POD_NAME=$(kubectl get pods -l app=cicd-kubernetes-mayte -o jsonpath='{.items[0].metadata.name}')
        echo "Pod encontrado: $POD_NAME"
        
        echo "Verificando que el pod esté corriendo..."
        kubectl get pod $POD_NAME
        
        echo "Probando conexión al pod directamente..."
        kubectl port-forward pod/$POD_NAME 8080:5000 &
        PF_PID=$!
        sleep 10
        
        echo "Haciendo request al pod..."
        curl -v http://localhost:8080/health || echo "Primera prueba falló, reintentando..."
        sleep 5
        curl -v http://localhost:8080/health || echo "No se pudo conectar al pod"
        
        kill $PF_PID || true
        echo "✅ Test completado"
    
    - name: 📋 Logs de pods
      if: always()
      run: |
        echo "=== LOGS DE TODOS LOS PODS ==="
        for pod in $(kubectl get pods -l app=cicd-kubernetes-mayte -o jsonpath='{.items[*].metadata.name}'); do
          echo "--- Logs del pod: $pod ---"
          kubectl logs $pod --tail=50 || echo "No se pudieron obtener logs"
          echo ""
        done
    
    - name: 📈 Recursos finales
      if: always()
      run: |
        echo "=== TODOS LOS RECURSOS ==="
        kubectl get all
        echo ""
        echo "=== EVENTOS RECIENTES ==="
        kubectl get events --sort-by='.lastTimestamp' | tail -20
    
    - name: 🎉 Resumen del pipeline
      if: success()
      run: |
        echo "========================================"
        echo "✅ PIPELINE CI/CD COMPLETADO CON ÉXITO"
        echo "========================================"
        echo "✅ Código validado"
        echo "✅ Tests unitarios pasados"
        echo "✅ Imagen Docker construida"
        echo "✅ Cluster Kubernetes iniciado"
        echo "✅ Aplicación desplegada en Kubernetes"
        echo "✅ Pods verificados"
        echo "✅ Service configurado"
        echo "========================================"
        echo "🚀 DESPLIEGUE KUBERNETES EXITOSO"
        echo "========================================"